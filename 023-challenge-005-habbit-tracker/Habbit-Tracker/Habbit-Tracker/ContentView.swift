//

import SwiftUI

// 1. Define the data structure for a habit.
// An ID, title and description will be required:

// 2. Store the habits in an array:

// 3. Define a data structure to map the activity to a counter that tracks the number of completed habits:


struct ContentView: View {
    var body: some View {
        VStack {

        }
        .padding()
    }
}

#Preview {
    ContentView()
}
